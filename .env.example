# Local (Prisma Dev): npx prisma dev --name mulher-segura --detach
DATABASE_URL="postgresql://postgres:postgres@localhost:51214/mulher_segura?sslmode=disable"

# Cookie de sessão — gere outro em cada ambiente (mín. 32 caracteres)
# openssl rand -base64 32
AUTH_SECRET="altere-em-cada-ambiente-com-openssl-rand-base64-32"
